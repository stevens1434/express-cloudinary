<h1>File Upload</h1>

<form enctype="multipart/form-data" action="/" method="POST">
  <input type="file" name="myFile">
  <input type="submit" class="btn btn-primary">
</form>

<h2>Images</h2>

<% images.forEach(function(image) { %>
  <% var imgUrl = cloudinary.url(image); %>
  <img src="<%= imgUrl %>">
<% }); %>
